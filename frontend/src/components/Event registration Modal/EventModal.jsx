import ModalClose from '@mui/joy/ModalClose';

const EventModal = () => {
  return (
    <Modal>
      <ModalDialog>
        <ModalClose />
        <Typography>Modal title</Typography>
      </ModalDialog>
    </Modal>
  );
};
